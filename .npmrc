# Ensure package-lock is generated
event-stream=false
package-lock=true

# Use exact versions in package-lock.json
save-exact=true

# Use npm ci for production builds
auto-install-peers=true

# Set the default branch
init.author.name="MK Worldwide"
init.license=ISC
