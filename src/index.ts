/**
 * Core library exports for PuppyThoth.
 * The greet function demonstrates the TypeScript scaffolding.
 */
export const greet = (name: string): string => {
  // Using template literals for readability and future localization hooks
  return `Hello, ${name}!`;
};
